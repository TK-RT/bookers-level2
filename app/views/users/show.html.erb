<div class="container">
	<div class="row">
		<div class="col-xs-3">
			<%= flash[:notice] %>
			<%= render "users/list", user: @user, book: @book %>
		</div>
		<h1>Books</h1>
		<div class="col-xs-9">
			<%= render "books/list", user: @user, books: @books %>
		</div>
	</div>
</div>